// components/PreviousButton.js
const PrevButton = ({ onClick }) => {
  return (
    <button className="swiper-button-prev" onClick={onClick}>
      Previous
    </button>
  );
};

export default PrevButton;
